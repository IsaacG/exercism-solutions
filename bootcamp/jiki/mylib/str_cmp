function my#str_cmp with a, b do
  for each letter in a indexed by idx do
    if letter == b[idx] do
    else if my#char_code(letter) < my#char_code(b[idx]) do
      return -1
    else do
      return 1
    end
  end
  return 0
end
